generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Phone {
  id     String     @id @default(uuid())
  number String
  brand  PhoneBrand
}

enum PhoneBrand {
  TIGO
  CLARO
  MOVISTAR
  OTHER
}
